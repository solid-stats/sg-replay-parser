export const gameTypes: GameType[] = ['sg', 'mace'];

export const replaysListFileName = 'replaysList.json';

export const configFolderName = 'config';

export const excludePlayersPath = `${configFolderName}/excludePlayers.json`;

export const requestsFilePath = `${configFolderName}/requests.csv`;
