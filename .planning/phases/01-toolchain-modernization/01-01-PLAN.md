---
phase: 01-toolchain-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
  - package.json
  - pnpm-lock.yaml
  - README.md
requirements:
  - TOOL-01
  - TOOL-02
autonomous: true
must_haves:
  truths:
    - Developer installs dependencies with `pnpm install --frozen-lockfile` and the repo rejects non-`pnpm` package managers.
    - Developer runs the main source-mode entrypoints through `pnpm run` scripts backed by `tsx`.
    - The repository again contains `docs/architecture.md`, so later work can follow the repo’s documented architecture-read requirement.
  artifacts:
    - path: docs/architecture.md
      provides: Restored runtime architecture source of truth covering `~/sg_stats`, entrypoints, and worker-thread responsibilities.
    - path: package.json
      provides: `packageManager`, `preinstall`, and pnpm-first `tsx` scripts for source-mode execution.
    - path: pnpm-lock.yaml
      provides: Deterministic dependency lock for the new package-manager policy.
  key_links:
    - from: package.json
      to: pnpm-lock.yaml
      via: deterministic install flow
      pattern: packageManager
    - from: package.json
      to: src/start.ts
      via: `pnpm run parse`
      pattern: tsx src/start.ts
    - from: package.json
      to: src/jobs/prepareReplaysList/start.ts
      via: `pnpm run generate-replays-list`
      pattern: tsx src/jobs/prepareReplaysList/start.ts
---

<objective>
Restore the missing architecture document and establish the pnpm-only, tsx-backed source execution surface.

Purpose: This isolates the low-risk but foundational manifest and documentation reset from the higher-risk build/runtime rewiring, while still satisfying the package-manager and source-entry decisions early.
Output: A restored `docs/architecture.md`, a pnpm-enforced manifest, `pnpm-lock.yaml`, and script-first `tsx` commands for the brownfield parser entrypoints.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-toolchain-modernization/01-CONTEXT.md
@.planning/phases/01-toolchain-modernization/01-RESEARCH.md
@.planning/phases/01-toolchain-modernization/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@package.json
@README.md
@src/start.ts
@src/schedule.ts
@src/jobs/prepareReplaysList/start.ts
@src/jobs/updateNameChangesCsv/start.ts
@src/jobs/generateMissionMakersList/start.ts
@src/jobs/generateMaceListHTML/start.ts
@src/!yearStatistics/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore `docs/architecture.md` from the planning baseline</name>
  <read_first>.planning/codebase/ARCHITECTURE.md, .planning/PROJECT.md, .planning/phases/01-toolchain-modernization/01-CONTEXT.md, docs/architecture.md</read_first>
  <files>docs/architecture.md</files>
  <action>Create `docs/architecture.md` in English using `.planning/codebase/ARCHITECTURE.md` as the concrete baseline. Include the current parser entrypoints `src/start.ts`, `src/schedule.ts`, `src/jobs/prepareReplaysList/start.ts`, and `src/!yearStatistics/index.ts`; describe the file-backed runtime rooted at `~/sg_stats`; document that replay parsing uses worker threads through `src/1 - replays/workers/parseReplayWorker.ts`; and state that Phase 1 must preserve published outputs and brownfield runtime semantics. Do not invent a new architecture or omit the missing-file recovery note.</action>
  <verify>
    <automated>test -f docs/architecture.md && rg -n "~/sg_stats|src/start.ts|src/schedule.ts|parseReplayWorker" docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>`docs/architecture.md` exists; `rg -n "~/sg_stats" docs/architecture.md` finds a match; `rg -n "src/start.ts|src/schedule.ts|src/jobs/prepareReplaysList/start.ts|src/!yearStatistics/index.ts" docs/architecture.md` finds matches; `rg -n "parseReplayWorker" docs/architecture.md` finds a match.</acceptance_criteria>
  <done>The repo’s required architecture document is present again and explicitly covers the brownfield parser runtime instead of remaining a missing reference.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the manifest to pnpm-only and define the script-first `tsx` entry surface</name>
  <read_first>package.json, README.md, src/start.ts, src/schedule.ts, src/jobs/prepareReplaysList/start.ts, src/jobs/updateNameChangesCsv/start.ts, src/jobs/generateMissionMakersList/start.ts, src/jobs/generateMaceListHTML/start.ts, src/!yearStatistics/index.ts</read_first>
  <files>package.json, pnpm-lock.yaml, README.md</files>
  <action>In `package.json`, add `"packageManager": "pnpm@10.33.0"` and `"preinstall": "npx only-allow pnpm"` per D-01 and D-02. Replace npm-first script wiring with pnpm-first scripts that execute source-mode entrypoints through `tsx`: `"parse": "tsx src/start.ts"`, `"schedule": "tsx src/schedule.ts"`, `"generate-replays-list": "tsx src/jobs/prepareReplaysList/start.ts"`, `"update-name-changes-csv": "tsx src/jobs/updateNameChangesCsv/start.ts"`, `"generate-mission-makers-list": "tsx src/jobs/generateMissionMakersList/start.ts"`, `"generate-mace-list": "tsx src/jobs/generateMaceListHTML/start.ts"`, and `"parse-new-year": "tsx src/!yearStatistics/index.ts"`. Add `tsx`, `typescript`, and `only-allow` to devDependencies, generate `pnpm-lock.yaml`, and update `README.md` so local run examples use `pnpm` instead of `npm` for the source-mode commands introduced here. Do not add npm fallbacks or leave any `npm run` script chaining in `package.json`.</action>
  <verify>
    <automated>pnpm install --frozen-lockfile && node -e "const pkg=require('./package.json'); if(pkg.packageManager!=='pnpm@10.33.0') process.exit(1); if(pkg.scripts.preinstall!=='npx only-allow pnpm') process.exit(1); const expected={parse:'tsx src/start.ts',schedule:'tsx src/schedule.ts','generate-replays-list':'tsx src/jobs/prepareReplaysList/start.ts','update-name-changes-csv':'tsx src/jobs/updateNameChangesCsv/start.ts','generate-mission-makers-list':'tsx src/jobs/generateMissionMakersList/start.ts','generate-mace-list':'tsx src/jobs/generateMaceListHTML/start.ts','parse-new-year':'tsx src/!yearStatistics/index.ts'}; for (const [key,val] of Object.entries(expected)) { if(pkg.scripts[key]!==val) process.exit(1); }" && test -f pnpm-lock.yaml && pnpm run parse --help >/dev/null 2>&1 && pnpm run generate-replays-list --help >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>`package.json` contains `"packageManager": "pnpm@10.33.0"` and `"preinstall": "npx only-allow pnpm"`; each of the seven source-mode script values matches the exact `tsx` command specified in the action; `pnpm-lock.yaml` exists; `rg -n "npm run" package.json` returns no matches; `README.md` contains `pnpm run generate-replays-list`.</acceptance_criteria>
  <done>The repo is pnpm-only, source-mode entrypoints are script-first through `pnpm run ...`, and TOOL-01 plus the source-execution half of TOOL-02 are explicitly implemented.</done>
</task>

</tasks>

<verification>
Run `pnpm install --frozen-lockfile`, `pnpm run parse --help`, and `pnpm run generate-replays-list --help`. Confirm `docs/architecture.md` exists and the README no longer tells developers to use npm for the local source-mode workflows.
</verification>

<success_criteria>
Phase 1 has an explicit pnpm-only foundation, concrete `tsx` source-entry scripts, and a restored architecture document before any build/runtime rewiring begins.
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-modernization/01-toolchain-modernization-01-SUMMARY.md`
</output>
