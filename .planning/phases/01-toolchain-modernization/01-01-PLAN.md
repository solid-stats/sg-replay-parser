---
phase: 01-toolchain-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
  - package.json
  - pnpm-lock.yaml
  - tsup.config.ts
  - tsconfig.json
  - tsconfig.build.json
  - src/0 - utils/runtimeConfig.ts
  - src/start.ts
  - src/schedule.ts
  - src/jobs/prepareReplaysList/start.ts
  - src/jobs/updateNameChangesCsv/start.ts
  - src/jobs/generateMissionMakersList/start.ts
  - src/jobs/generateMaceListHTML/start.ts
  - src/!yearStatistics/index.ts
  - src/1 - replays/workers/workerPool.ts
  - src/1 - replays/workers/parseReplayWorker.ts
  - ecosystem.config.cjs
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
autonomous: true
must_haves:
  truths:
    - Developer installs dependencies with `pnpm install --frozen-lockfile` and the repo rejects non-pnpm installs.
    - Developer can run parser and replay-list source entrypoints through `pnpm run` scripts backed by `tsx`.
    - Maintainer can build ESM-oriented distributable output and still get the operational files under `dist/start.js`, `dist/schedule.js`, and the job entrypoint paths.
  artifacts:
    - path: docs/architecture.md
      provides: Current runtime architecture baseline restored for repo-required read-first workflow and missing-doc risk closure.
    - path: package.json
      provides: `packageManager`, `preinstall`, `pnpm` scripts, `tsx`/`tsup` dependencies, and no npm-first command surface.
    - path: tsup.config.ts
      provides: Multi-entry build for `src/start.ts`, `src/schedule.ts`, `src/jobs/prepareReplaysList/start.ts`, `src/jobs/updateNameChangesCsv/start.ts`, `src/jobs/generateMissionMakersList/start.ts`, `src/jobs/generateMaceListHTML/start.ts`, `src/!yearStatistics/index.ts`, and `src/1 - replays/workers/parseReplayWorker.ts`.
  key_links:
    - from: package.json
      to: tsup.config.ts
      via: `pnpm build`
      pattern: '"build": "tsup --config tsup.config.ts"'
    - from: src/1 - replays/workers/workerPool.ts
      to: dist/1 - replays/workers/parseReplayWorker.js
      via: worker thread path resolution
      pattern: parseReplayWorker
    - from: ecosystem.config.cjs
      to: dist/schedule.js
      via: PM2 runtime script
      pattern: dist/schedule.js
---

<objective>
Establish the pnpm-only, tsx-driven, tsup-built foundation for the parser while restoring the missing runtime architecture document.

Purpose: Satisfy the package-manager, source-entry, and build-output requirements first so later strictness and test-runner work land on the final runtime surface instead of the legacy npm/tsc shape.
Output: A pnpm-enforced package manifest, a multi-entry tsup build config, source-mode tsx scripts, preserved dist entrypoints, and a restored `docs/architecture.md`.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-toolchain-modernization/01-CONTEXT.md
@.planning/phases/01-toolchain-modernization/01-RESEARCH.md
@.planning/phases/01-toolchain-modernization/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@package.json
@tsconfig.json
@tsconfig.build.json
@src/start.ts
@src/schedule.ts
@src/jobs/prepareReplaysList/start.ts
@src/1 - replays/workers/parseReplayWorker.ts
@ecosystem.config.cjs

<interfaces>
From `src/1 - replays/workers/parseReplayWorker.ts`:
```ts
export const runParseTask = async <TTaskId extends string>(
  task: ParseReplayTaskMessage<TTaskId>,
): Promise<ParseReplayTaskResponseMessage<TTaskId>> => { ... }
```

Operational entry files that must remain build targets:
```text
src/start.ts
src/schedule.ts
src/jobs/prepareReplaysList/start.ts
src/jobs/updateNameChangesCsv/start.ts
src/jobs/generateMissionMakersList/start.ts
src/jobs/generateMaceListHTML/start.ts
src/!yearStatistics/index.ts
src/1 - replays/workers/parseReplayWorker.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore the required architecture document and define the pnpm-only manifest surface</name>
  <read_first>.planning/codebase/ARCHITECTURE.md, .planning/PROJECT.md, .planning/phases/01-toolchain-modernization/01-CONTEXT.md, package.json, README.md, docs/architecture.md</read_first>
  <files>docs/architecture.md, package.json, pnpm-lock.yaml</files>
  <action>Create `docs/architecture.md` in English using `.planning/codebase/ARCHITECTURE.md` as the source baseline, explicitly describing the runtime entrypoints, `~/sg_stats` file-backed runtime model, worker-thread parsing, and the brownfield constraint that published outputs must remain stable. In `package.json`, add `"packageManager": "pnpm@10.33.0"` and `"preinstall": "npx only-allow pnpm"` per D-01 and D-02, replace npm-based scripts with pnpm-first script names, and add source-mode commands backed by `tsx` for `src/start.ts`, `src/schedule.ts`, `src/jobs/prepareReplaysList/start.ts`, `src/jobs/updateNameChangesCsv/start.ts`, `src/jobs/generateMissionMakersList/start.ts`, `src/jobs/generateMaceListHTML/start.ts`, and `src/!yearStatistics/index.ts`. Add devDependencies for `pnpm` workflow support, `tsx`, `tsup`, modern `typescript`, and `only-allow`, and remove the legacy npm lockfile path by generating `pnpm-lock.yaml` during execution. Do not keep npm fallback scripts or npm-centric wording in the package manifest.</action>
  <verify>
    <automated>pnpm install --frozen-lockfile && node -e "const pkg=require('./package.json'); if(pkg.packageManager!=='pnpm@10.33.0') process.exit(1); if(pkg.scripts.preinstall!=='npx only-allow pnpm') process.exit(1); for (const key of ['parse','schedule','generate-replays-list','update-name-changes-csv','generate-mace-list','generate-mission-makers-list','parse-new-year']) { if(!pkg.scripts[key] || !pkg.scripts[key].includes('tsx')) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>`docs/architecture.md` exists and contains the strings `~/sg_stats`, `src/start.ts`, and `src/schedule.ts`; `package.json` contains `"packageManager": "pnpm@10.33.0"` and `"preinstall": "npx only-allow pnpm"`; `package.json` contains no script value matching `npm run`; `pnpm-lock.yaml` exists.</acceptance_criteria>
  <done>The repo has a committed architecture document again, `pnpm` is the only supported package manager per D-01/D-02, and the source-mode command surface is script-first per D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the legacy tsc build with an explicit tsup multi-entry build that preserves runtime entrypoints</name>
  <read_first>package.json, tsconfig.json, tsconfig.build.json, src/start.ts, src/schedule.ts, src/jobs/prepareReplaysList/start.ts, src/jobs/updateNameChangesCsv/start.ts, src/jobs/generateMissionMakersList/start.ts, src/jobs/generateMaceListHTML/start.ts, src/!yearStatistics/index.ts, src/1 - replays/workers/parseReplayWorker.ts, src/1 - replays/workers/workerPool.ts</read_first>
  <files>package.json, tsup.config.ts, tsconfig.json, tsconfig.build.json, src/1 - replays/workers/workerPool.ts</files>
  <action>Create `tsup.config.ts` with the exact entry array `['src/start.ts','src/schedule.ts','src/jobs/prepareReplaysList/start.ts','src/jobs/updateNameChangesCsv/start.ts','src/jobs/generateMissionMakersList/start.ts','src/jobs/generateMaceListHTML/start.ts','src/!yearStatistics/index.ts','src/1 - replays/workers/parseReplayWorker.ts']`, `format: ['esm']`, `target: 'node20'`, `outDir: 'dist'`, `clean: true`, `sourcemap: true`, `splitting: false`, and `dts: false` per D-06 and D-07. Convert `tsconfig.json` into the strict no-emit base config for NodeNext/ES2022 consumption by `tsx` and `tsup`, and leave `tsconfig.build.json` as the build-specific companion if tsup needs overrides. Update `package.json` so `pnpm build` runs `tsup --config tsup.config.ts` and add a separate `pnpm typecheck` command. Update worker-path resolution in `src/1 - replays/workers/workerPool.ts` so the built scheduler/parser still resolves the emitted `dist/1 - replays/workers/parseReplayWorker.js` path correctly under ESM output instead of assuming the old CommonJS `tsc` layout.</action>
  <verify>
    <automated>pnpm build && test -f dist/start.js && test -f dist/schedule.js && test -f "dist/jobs/prepareReplaysList/start.js" && test -f "dist/jobs/updateNameChangesCsv/start.js" && test -f "dist/jobs/generateMissionMakersList/start.js" && test -f "dist/jobs/generateMaceListHTML/start.js" && test -f "dist/!yearStatistics/index.js" && test -f "dist/1 - replays/workers/parseReplayWorker.js"</automated>
  </verify>
  <acceptance_criteria>`tsup.config.ts` contains each of the eight entry paths verbatim plus `format: ['esm']`; `package.json` contains `"build": "tsup --config tsup.config.ts"` and `"typecheck"`; `tsconfig.json` contains `"module": "NodeNext"` and `"noEmit": true`; `dist/start.js`, `dist/schedule.js`, and `dist/1 - replays/workers/parseReplayWorker.js` exist after `pnpm build`.</acceptance_criteria>
  <done>The repository builds through tsup instead of legacy `tsc` emission, and the operational `dist/*` entrypoints remain usable on the new ESM-oriented build surface.</done>
</task>

<task type="auto">
  <name>Task 3: Move entrypoint wrappers and runtime startup assumptions onto the new source/build surface</name>
  <read_first>src/start.ts, src/schedule.ts, src/jobs/prepareReplaysList/start.ts, src/jobs/updateNameChangesCsv/start.ts, src/jobs/generateMissionMakersList/start.ts, src/jobs/generateMaceListHTML/start.ts, src/!yearStatistics/index.ts, ecosystem.config.cjs, .planning/codebase/ARCHITECTURE.md</read_first>
  <files>src/start.ts, src/schedule.ts, src/jobs/prepareReplaysList/start.ts, src/jobs/updateNameChangesCsv/start.ts, src/jobs/generateMissionMakersList/start.ts, src/jobs/generateMaceListHTML/start.ts, src/!yearStatistics/index.ts, ecosystem.config.cjs, src/0 - utils/runtimeConfig.ts</files>
  <action>Adjust all top-level entry wrappers and runtime assumptions needed for the NodeNext/tsx/tsup surface created in Tasks 1-2. Keep the script-first UX from D-04, but ensure each entry file remains executable from source via `tsx` and from build output via Node on the emitted `dist/*.js` files. Update `ecosystem.config.cjs` to keep PM2 pointed at `dist/schedule.js` or the documented replacement path chosen under D-07, and make any ESM-safe runtime config changes needed so source-mode entrypoints, worker threads, and the yearly pipeline continue to load under Node 20 semantics without falling back to CommonJS-specific assumptions.</action>
  <verify>
    <automated>pnpm run parse -- --help >/dev/null 2>&1 || true; pnpm run generate-replays-list -- --help >/dev/null 2>&1 || true; pnpm build && node dist/start.js --help >/dev/null 2>&1 || true; node dist/schedule.js >/dev/null 2>&1 & pid=$!; sleep 2; kill $pid >/dev/null 2>&1 || true; wait $pid >/dev/null 2>&1 || true; node -e "const fs=require('fs'); const text=fs.readFileSync('ecosystem.config.cjs','utf8'); if(!text.includes('dist/schedule.js')) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>`ecosystem.config.cjs` contains `dist/schedule.js`; each touched `start.ts` wrapper still sets `process.exitCode = 1` on fatal failure; no touched entry file imports `require(`; `src/1 - replays/workers/workerPool.ts` references `parseReplayWorker` with a `.js` build target or equivalent ESM-safe resolution.</acceptance_criteria>
  <done>The parser’s runtime entrypoints are executable on the new toolchain and the build artifacts still expose the operational files later plans will document and validate.</done>
</task>

</tasks>

<verification>
Run `pnpm install --frozen-lockfile`, `pnpm typecheck`, and `pnpm build`. Confirm the worker file, scheduler entry, parser entry, and job start files exist under `dist/` and that `docs/architecture.md` is restored.
</verification>

<success_criteria>
Phase 1 has a pnpm-only foundation, tsx-backed source scripts, a tsup multi-entry build that emits the operational dist paths, and the missing architecture document has been restored so later tasks can follow repo-required documentation hygiene.
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-modernization/01-toolchain-modernization-01-SUMMARY.md`
</output>
