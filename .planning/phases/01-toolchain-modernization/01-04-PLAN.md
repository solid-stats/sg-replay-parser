---
phase: 01-toolchain-modernization
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - package.json
  - vitest.config.ts
  - src/!tests/**/*.test.ts
  - src/!tests/**/*.snap
  - src/!tests/utils/**/*.ts
  - README.md
  - .github/workflows/ci.yml
  - deploy/remote-deploy.sh
  - ecosystem.config.cjs
requirements:
  - TOOL-06
  - TOOL-07
autonomous: true
must_haves:
  truths:
    - Maintainer runs the full suite with Vitest and coverage on the modernized toolchain.
    - Maintainer runs CI/build/test commands with pnpm instead of npm.
    - Operator follows the documented production command surface without npm-specific behavior.
  artifacts:
    - path: vitest.config.ts
      provides: Vitest node configuration with V8 coverage for the existing test layout.
    - path: README.md
      provides: pnpm-first local, CI, deploy, and PM2 documentation.
    - path: .github/workflows/ci.yml
      provides: CI pipeline using pnpm install, lint, typecheck, test, and build commands.
  key_links:
    - from: vitest.config.ts
      to: src/!tests/**/*.test.ts
      via: Vitest `vi.*` API and node environment
      pattern: vitest
    - from: deploy/remote-deploy.sh
      to: package.json
      via: production build commands
      pattern: pnpm
    - from: README.md
      to: ecosystem.config.cjs
      via: documented PM2 runtime path
      pattern: dist/schedule.js
---

<objective>
Replace Jest with Vitest and finish the operator-facing pnpm command migration across docs and automation.

Purpose: The test-runner migration remains the highest-risk part of Phase 1, and TOOL-07 must land on the final build/runtime and lint/typecheck surface rather than a moving target.
Output: A Vitest-only suite, pnpm-based CI/deploy/docs, and a final operator command surface with no required npm semantics.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-toolchain-modernization/01-CONTEXT.md
@.planning/phases/01-toolchain-modernization/01-RESEARCH.md
@.planning/phases/01-toolchain-modernization/01-VALIDATION.md
@.planning/codebase/TESTING.md
@package.json
@jest.config.js
@README.md
@.github/workflows/ci.yml
@deploy/remote-deploy.sh
@ecosystem.config.cjs

<interfaces>
Current Jest-specific surface that must be migrated:
```text
38 test files
3 snapshot files
jest.mock(...)
jest.spyOn(...)
jest.resetModules(...)
jest.requireActual(...)
jest.useFakeTimers()
jest.setSystemTime()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce Vitest config and migrate the Jest-specific suite surface</name>
  <read_first>package.json, jest.config.js, .planning/codebase/TESTING.md, src/!tests/unit-tests/index.test.ts, src/!tests/unit-tests/schedule.test.ts, src/!tests/unit-tests/0 - utils/request.test.ts, src/!tests/unit-tests/jobs/prepareReplaysList/startFetchingReplays.test.ts, src/!tests/unit-tests/1 - replays, 2 - parseReplayInfo/workers/parseReplayWorker.test.ts</read_first>
  <files>package.json, vitest.config.ts, src/!tests/**/*.test.ts, src/!tests/**/*.snap, src/!tests/utils/**/*.ts</files>
  <behavior>
    - Vitest runs in node mode over the existing `src/!tests/**/*.test.ts` layout with V8 coverage.
    - Jest-specific mock, timer, and module-reset APIs are replaced with `vi.*` equivalents.
    - Snapshot and orchestration tests remain deterministic and behavior-focused.
  </behavior>
  <action>Remove Jest-only dependencies and scripts from `package.json`, add `vitest` and `@vitest/coverage-v8`, and create `vitest.config.ts` with `test.environment = 'node'`, `globals = true`, `include = ['src/!tests/**/*.test.ts']`, and coverage thresholds matching the existing 100% global threshold policy unless the suite proves a concrete incompatible case that must be documented. Convert test files and helpers from `jest.*` to `vi.*`: `jest.mock` -> `vi.mock`, `jest.spyOn` -> `vi.spyOn`, `jest.useFakeTimers` -> `vi.useFakeTimers`, `jest.setSystemTime` -> `vi.setSystemTime`, `jest.resetModules` -> `vi.resetModules`, and `jest.requireActual` -> `vi.importActual`. Fix hoisting and import-order issues explicitly in the worker and prepare-replays suites instead of adding broad compatibility shims.</action>
  <verify>
    <automated>pnpm test && pnpm exec vitest run --coverage</automated>
  </verify>
  <acceptance_criteria>`vitest.config.ts` exists and contains `environment: 'node'`, `include: ['src/!tests/**/*.test.ts']`, and `provider: 'v8'`; `package.json` contains a Vitest-based `test` script and no `jest` script; `rg -n "jest\\." src/!tests` returns no matches; `pnpm exec vitest run --coverage` exits 0.</acceptance_criteria>
  <done>Phase 1 ends in a Vitest-only state with the existing deterministic suite migrated to the new runner.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README, CI, deploy, and PM2 command surfaces to the final pnpm workflow</name>
  <read_first>README.md, .github/workflows/ci.yml, deploy/remote-deploy.sh, ecosystem.config.cjs, package.json, docs/architecture.md</read_first>
  <files>README.md, .github/workflows/ci.yml, deploy/remote-deploy.sh, ecosystem.config.cjs, package.json</files>
  <action>Update every operator-facing automation and document to the final Phase 1 command surface from D-03, D-04, D-05, and D-08. In `.github/workflows/ci.yml`, replace `npm ci`, `npm run lint`, `npm run tsc`, and `npm run test` with pnpm equivalents, add Corepack or pnpm setup if needed, and include `pnpm build` in the verification chain. In `deploy/remote-deploy.sh`, replace `npm ci` and `npm run build-dist` with `pnpm install --frozen-lockfile` and `pnpm build`. In `README.md`, replace all required npm commands with the final pnpm commands, document the source-mode and built-runtime commands defined in earlier plans, and keep PM2 pointed at `dist/schedule.js`. Do not leave any required operational step depending on npm semantics.</action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm build && ! rg -n "npm (run|ci)|\\bnpm\\b" README.md .github/workflows/ci.yml deploy/remote-deploy.sh ecosystem.config.cjs</automated>
  </verify>
  <acceptance_criteria>`README.md`, `.github/workflows/ci.yml`, and `deploy/remote-deploy.sh` contain `pnpm` commands and no required `npm` commands; `.github/workflows/ci.yml` contains `pnpm build`; `deploy/remote-deploy.sh` contains `pnpm install --frozen-lockfile` and `pnpm build`; `ecosystem.config.cjs` still contains `dist/schedule.js`.</acceptance_criteria>
  <done>Operators and maintainers use a single pnpm-based command surface across docs, CI, deploy, and PM2, satisfying TOOL-07 on top of the finalized runtime/tooling stack.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm typecheck && pnpm test && pnpm exec vitest run --coverage && pnpm build`. Then grep README, CI, deploy, and PM2 files to confirm required operator flows no longer depend on npm.
</verification>

<success_criteria>
The repository finishes Phase 1 with Vitest as the only supported test runner and a fully documented pnpm-based operator workflow across README, CI, deploy, and PM2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-modernization/01-toolchain-modernization-04-SUMMARY.md`
</output>
