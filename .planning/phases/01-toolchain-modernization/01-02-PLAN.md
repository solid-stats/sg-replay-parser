---
phase: 01-toolchain-modernization
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - tsup.config.ts
  - tsconfig.json
  - tsconfig.build.json
  - src/start.ts
  - src/schedule.ts
  - src/jobs/prepareReplaysList/start.ts
  - src/jobs/updateNameChangesCsv/start.ts
  - src/jobs/generateMissionMakersList/start.ts
  - src/jobs/generateMaceListHTML/start.ts
  - src/!yearStatistics/index.ts
  - src/1 - replays/workers/workerPool.ts
  - src/1 - replays/workers/parseReplayWorker.ts
  - ecosystem.config.cjs
requirements:
  - TOOL-03
autonomous: true
must_haves:
  truths:
    - Maintainer builds the repo with `pnpm build` and gets the expected `dist/*.js` runtime entrypoints.
    - Built parser and replay-list entrypoints execute successfully under Node from the emitted `dist/` tree.
    - PM2 and worker-thread wiring point at the built runtime files, not the removed CommonJS `tsc` assumptions.
  artifacts:
    - path: tsup.config.ts
      provides: Multi-entry ESM build for parser, scheduler, jobs, yearly stats, and worker entrypoints.
    - path: src/1 - replays/workers/workerPool.ts
      provides: ESM-safe worker path resolution for the built `parseReplayWorker.js`.
    - path: ecosystem.config.cjs
      provides: PM2 runtime path matching the emitted scheduler build output.
  key_links:
    - from: package.json
      to: tsup.config.ts
      via: `pnpm build`
      pattern: tsup --config tsup.config.ts
    - from: src/1 - replays/workers/workerPool.ts
      to: dist/1 - replays/workers/parseReplayWorker.js
      via: worker thread startup
      pattern: parseReplayWorker
    - from: ecosystem.config.cjs
      to: dist/schedule.js
      via: PM2 runtime
      pattern: dist/schedule.js
---

<objective>
Replace the old `tsc` build with the actual production build/runtime wiring for the modernized toolchain.

Purpose: This keeps TOOL-03 isolated from manifest setup and strictness cleanup, and it gives the phase a fail-fast build/runtime checkpoint before lint and test migration start rewriting more of the repo.
Output: A `tsup` multi-entry build, NodeNext build config, ESM-safe worker/runtime entry wiring, and PM2 configuration that matches the emitted `dist/` layout.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-toolchain-modernization/01-CONTEXT.md
@.planning/phases/01-toolchain-modernization/01-RESEARCH.md
@.planning/phases/01-toolchain-modernization/01-VALIDATION.md
@package.json
@tsconfig.json
@tsconfig.build.json
@src/start.ts
@src/schedule.ts
@src/jobs/prepareReplaysList/start.ts
@src/jobs/updateNameChangesCsv/start.ts
@src/jobs/generateMissionMakersList/start.ts
@src/jobs/generateMaceListHTML/start.ts
@src/!yearStatistics/index.ts
@src/1 - replays/workers/workerPool.ts
@src/1 - replays/workers/parseReplayWorker.ts
@ecosystem.config.cjs

<interfaces>
Build entry files that must emit matching `dist/*.js` targets:
```text
src/start.ts
src/schedule.ts
src/jobs/prepareReplaysList/start.ts
src/jobs/updateNameChangesCsv/start.ts
src/jobs/generateMissionMakersList/start.ts
src/jobs/generateMaceListHTML/start.ts
src/!yearStatistics/index.ts
src/1 - replays/workers/parseReplayWorker.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `tsup` multi-entry build and NodeNext build configs</name>
  <read_first>package.json, tsconfig.json, tsconfig.build.json, .planning/phases/01-toolchain-modernization/01-RESEARCH.md, src/start.ts, src/schedule.ts, src/jobs/prepareReplaysList/start.ts, src/1 - replays/workers/parseReplayWorker.ts</read_first>
  <files>package.json, tsup.config.ts, tsconfig.json, tsconfig.build.json</files>
  <action>Create `tsup.config.ts` with the exact `entry` array `['src/start.ts','src/schedule.ts','src/jobs/prepareReplaysList/start.ts','src/jobs/updateNameChangesCsv/start.ts','src/jobs/generateMissionMakersList/start.ts','src/jobs/generateMaceListHTML/start.ts','src/!yearStatistics/index.ts','src/1 - replays/workers/parseReplayWorker.ts']`, `format: ['esm']`, `target: 'node20'`, `outDir: 'dist'`, `clean: true`, `sourcemap: true`, `splitting: false`, `dts: false`, and `shims: false`. Update `package.json` so `"build": "tsup --config tsup.config.ts"` and add explicit built-runtime scripts `"parse:dist": "node dist/start.js"` and `"schedule:dist": "node dist/schedule.js"`. Convert `tsconfig.json` into the NodeNext no-emit base for the new toolchain and keep `tsconfig.build.json` as the build companion instead of using legacy CommonJS `tsc` emission.</action>
  <verify>
    <automated>pnpm build && test -f dist/start.js && test -f dist/schedule.js && test -f "dist/jobs/prepareReplaysList/start.js" && test -f "dist/jobs/updateNameChangesCsv/start.js" && test -f "dist/jobs/generateMissionMakersList/start.js" && test -f "dist/jobs/generateMaceListHTML/start.js" && test -f "dist/!yearStatistics/index.js" && test -f "dist/1 - replays/workers/parseReplayWorker.js"</automated>
  </verify>
  <acceptance_criteria>`tsup.config.ts` exists and contains all eight source entry paths verbatim plus `format: ['esm']`; `package.json` contains `"build": "tsup --config tsup.config.ts"`, `"parse:dist": "node dist/start.js"`, and `"schedule:dist": "node dist/schedule.js"`; `tsconfig.json` contains `"module": "NodeNext"` and `"noEmit": true`; each expected `dist/*.js` path exists after `pnpm build`.</acceptance_criteria>
  <done>The repo now emits the required built runtime entrypoints through `tsup` instead of the legacy `tsc` pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire entrypoint wrappers, worker startup, and PM2 to the emitted build layout with fail-fast runtime checks</name>
  <read_first>src/start.ts, src/schedule.ts, src/jobs/prepareReplaysList/start.ts, src/jobs/updateNameChangesCsv/start.ts, src/jobs/generateMissionMakersList/start.ts, src/jobs/generateMaceListHTML/start.ts, src/!yearStatistics/index.ts, src/1 - replays/workers/workerPool.ts, src/1 - replays/workers/parseReplayWorker.ts, ecosystem.config.cjs, package.json</read_first>
  <files>src/start.ts, src/schedule.ts, src/jobs/prepareReplaysList/start.ts, src/jobs/updateNameChangesCsv/start.ts, src/jobs/generateMissionMakersList/start.ts, src/jobs/generateMaceListHTML/start.ts, src/!yearStatistics/index.ts, src/1 - replays/workers/workerPool.ts, src/1 - replays/workers/parseReplayWorker.ts, ecosystem.config.cjs</files>
  <action>Update the top-level runtime files so the source-mode `tsx` entrypoints from Plan 01 and the built `dist/*.js` entrypoints from Task 1 both work under Node 20 ESM semantics. In `src/1 - replays/workers/workerPool.ts`, replace any CommonJS- or `__dirname`-style worker path assumptions with ESM-safe resolution that targets the emitted `parseReplayWorker.js` file. Keep each entry wrapper’s fatal logging and `process.exitCode = 1` behavior intact. Update `ecosystem.config.cjs` so PM2 launches `dist/schedule.js` and no other runtime file. Verify the runtime surface with fail-fast smoke commands only: do not use `|| true`, do not swallow non-zero exits, and do not treat missing output as success.</action>
  <verify>
    <automated>pnpm build && pnpm run parse:dist --help >/dev/null 2>&1 && node dist/jobs/prepareReplaysList/start.js --help >/dev/null 2>&1 && timeout 5s node dist/schedule.js >/dev/null 2>&1 && node -e "const fs=require('fs'); const text=fs.readFileSync('ecosystem.config.cjs','utf8'); if(!text.includes('dist/schedule.js')) process.exit(1);" && rg -n "parseReplayWorker.*\\.js|new URL\\(" "src/1 - replays/workers/workerPool.ts"</automated>
  </verify>
  <acceptance_criteria>`ecosystem.config.cjs` contains `script: 'dist/schedule.js'`; `src/1 - replays/workers/workerPool.ts` contains either `parseReplayWorker.js` or `new URL(` for ESM-safe worker resolution; each touched `start.ts` wrapper still contains `process.exitCode = 1`; the automated smoke command exits 0 without any `|| true` masking.</acceptance_criteria>
  <done>The build output is not only present but runnable, and PM2 plus worker-thread wiring match the emitted runtime layout with fail-fast verification.</done>
</task>

</tasks>

<verification>
Run `pnpm build`, `pnpm run parse:dist --help`, `node dist/jobs/prepareReplaysList/start.js --help`, and `timeout 5s node dist/schedule.js`. All commands must succeed directly; no masking or fallback success is allowed.
</verification>

<success_criteria>
The repo has a working tsup-built runtime surface with preserved dist entrypoints, ESM-safe worker wiring, and PM2 configuration aligned to the emitted scheduler entrypoint.
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-modernization/01-toolchain-modernization-02-SUMMARY.md`
</output>
