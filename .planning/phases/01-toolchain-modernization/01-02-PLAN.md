---
phase: 01-toolchain-modernization
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - eslint.config.mjs
  - package.json
  - tsconfig.json
  - tsconfig.build.json
  - .eslintignore
  - src/**/*.ts
requirements:
  - TOOL-04
  - TOOL-05
autonomous: true
must_haves:
  truths:
    - Maintainer runs `pnpm typecheck` successfully against a strict backend TypeScript configuration.
    - Maintainer runs `pnpm lint` successfully against a flat ESLint config trimmed to backend TypeScript instead of React/Next/browser rules.
    - The codebase no longer depends on the legacy `.eslintrc` and old TypeScript compiler settings to pass quality gates.
  artifacts:
    - path: eslint.config.mjs
      provides: Flat ESLint configuration using `@eslint/js` and `typescript-eslint` with backend-only globals and ignores.
    - path: tsconfig.json
      provides: Strict NodeNext no-emit typecheck configuration with modern strictness flags.
    - path: package.json
      provides: `lint` and `typecheck` commands pointed at the new config surface.
  key_links:
    - from: eslint.config.mjs
      to: tsconfig.json
      via: typed lint parser options
      pattern: projectService
    - from: package.json
      to: eslint.config.mjs
      via: `pnpm lint`
      pattern: eslint
---

<objective>
Finish the strict TypeScript and ESLint modernization on top of the new toolchain foundation.

Purpose: TOOL-04 and TOOL-05 are quality gates for the rest of the milestone. They need their own cleanup pass because the current repo mixes outdated compiler settings, Airbnb config, and backend-unfriendly frontend lint presets.
Output: A strict Node-focused `tsconfig`, a backend-only ESLint flat config, and source fixes required to make `pnpm typecheck` and `pnpm lint` pass without weakening rigor.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-toolchain-modernization/01-CONTEXT.md
@.planning/phases/01-toolchain-modernization/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@package.json
@tsconfig.json
@tsconfig.build.json
@.eslintrc
@new_config_files/eslint.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the Airbnb ESLint setup with a backend-only flat config derived from `new_config_files`</name>
  <read_first>.eslintrc, .eslintignore, new_config_files/eslint.config.ts, .planning/phases/01-toolchain-modernization/01-RESEARCH.md, package.json</read_first>
  <files>eslint.config.mjs, package.json, .eslintignore</files>
  <action>Create `eslint.config.mjs` from the flat-config structure in `new_config_files/eslint.config.ts`, but keep only backend TypeScript concerns per D-09 and TOOL-05: use `@eslint/js`, `typescript-eslint`, `globals.node`, typed parser options with `projectService: true`, and ignore `dist/**`, `coverage/**`, `node_modules/**`, and runtime-generated output directories. Remove React, Next.js, JSX, browser globals, MobX, and frontend-only plugins entirely. Keep rules that preserve existing repo conventions such as `func-style`, `import/order`, `no-duplicate-imports`, `@typescript-eslint/no-floating-promises`, and `@typescript-eslint/no-misused-promises`. Update `package.json` so `pnpm lint` runs the flat config over `src/**/*.ts`, config files, and test files. Retire `.eslintrc` and `.eslintignore` from active use rather than carrying them as primary config.</action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>`eslint.config.mjs` exists and contains `globals.node`, `projectService: true`, and ignores for `dist/**` and `coverage/**`; `eslint.config.mjs` does not contain `react`, `next`, `jsx-a11y`, or `globals.browser`; `package.json` contains a `lint` script that does not invoke `.eslintrc`.</acceptance_criteria>
  <done>The repo lints through a backend-only flat ESLint config and no longer depends on the legacy Airbnb `.eslintrc` stack.</done>
</task>

<task type="auto">
  <name>Task 2: Harden TypeScript to a strict Node backend baseline and fix resulting source violations</name>
  <read_first>tsconfig.json, tsconfig.build.json, package.json, .planning/phases/01-toolchain-modernization/01-RESEARCH.md, src/0 - utils/runtimeConfig.ts, src/schedule.ts, src/1 - replays/workers/parseReplayWorker.ts</read_first>
  <files>tsconfig.json, tsconfig.build.json, package.json, src/**/*.ts</files>
  <action>Update `tsconfig.json` to the strongest practical backend baseline required by D-09 and D-10: keep `target: 'ES2022'`, `module: 'NodeNext'`, `moduleResolution: 'NodeNext'`, `strict: true`, `noEmit: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`, `noImplicitOverride: true`, `useUnknownInCatchVariables: true`, `resolveJsonModule: true`, and `types: ['node']`. Keep `typeRoots` support for `src/0 - types` if still required by the ambient model. Update any source files that fail under the stricter config by removing implicit-any/catch-any behavior, tightening nullable branches, and replacing compatibility casts with precise types. Keep the repo’s function-expression and deterministic backend style intact; do not weaken strict flags to avoid cleanup.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>`tsconfig.json` contains `"strict": true`, `"noEmit": true`, `"noUncheckedIndexedAccess": true`, `"exactOptionalPropertyTypes": true`, and `"useUnknownInCatchVariables": true`; `package.json` contains `"typecheck"`; `pnpm typecheck` exits 0.</acceptance_criteria>
  <done>The repository passes strict backend type checking on the new NodeNext toolchain without lowering TypeScript rigor.</done>
</task>

</tasks>

<verification>
Run `pnpm typecheck && pnpm lint`. Capture any remaining lint/type issues in the summary if they require follow-up, but do not weaken rules to force a green result.
</verification>

<success_criteria>
The repo has a backend-only flat ESLint setup and a genuinely strict TypeScript baseline, and both quality gates pass on the modernized toolchain.
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-modernization/01-toolchain-modernization-02-SUMMARY.md`
</output>
