---
phase: 01-toolchain-modernization
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - package.json
  - vitest.config.ts
  - src/!tests/**/*.test.ts
  - src/!tests/**/*.snap
  - src/!tests/utils/**/*.ts
  - README.md
  - .github/workflows/ci.yml
  - deploy/remote-deploy.sh
  - ecosystem.config.cjs
requirements:
  - TOOL-06
  - TOOL-07
autonomous: true
must_haves:
  truths:
    - Maintainer runs the full test suite with Vitest and gets deterministic, meaningful results.
    - Maintainer runs the CI/build/test command set with pnpm instead of npm.
    - Operator follows the documented production command surface without needing npm semantics.
  artifacts:
    - path: vitest.config.ts
      provides: Vitest node test configuration, coverage settings, and setup compatible with the existing suite.
    - path: README.md
      provides: pnpm-first developer and operator instructions, including deployment commands.
    - path: .github/workflows/ci.yml
      provides: CI pipeline using pnpm install, lint, typecheck, test, and build commands.
  key_links:
    - from: vitest.config.ts
      to: src/!tests/**/*.test.ts
      via: Jest-to-Vitest compatibility API
      pattern: from 'vitest'
    - from: deploy/remote-deploy.sh
      to: package.json
      via: production build commands
      pattern: pnpm
    - from: README.md
      to: ecosystem.config.cjs
      via: documented PM2/deploy command surface
      pattern: pm2
---

<objective>
Replace Jest with Vitest and finish the operator-facing command migration across docs and automation.

Purpose: The test-runner migration is the highest-risk part of Phase 1 and it also gates the final command-surface documentation required by TOOL-07. Keeping it isolated avoids mixing broad mock-conversion work with foundational build changes.
Output: A Vitest-only test setup, migrated tests and snapshots, and pnpm-based README/CI/deploy/PM2 command documentation.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-toolchain-modernization/01-CONTEXT.md
@.planning/phases/01-toolchain-modernization/01-RESEARCH.md
@.planning/phases/01-toolchain-modernization/01-VALIDATION.md
@.planning/codebase/TESTING.md
@package.json
@jest.config.js
@README.md
@.github/workflows/ci.yml
@deploy/remote-deploy.sh
@ecosystem.config.cjs

<interfaces>
Current test surface:
```text
38 test files
3 snapshot files
jest.mock(...) across orchestration and boundary suites
jest.spyOn(...) across utility/statistics/worker suites
jest.resetModules(...) in parseReplayWorker tests
jest.requireActual(...) in prepareReplaysList tests
jest.useFakeTimers() + jest.setSystemTime() in request tests
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce Vitest config and migrate the Jest-specific suite surface</name>
  <read_first>package.json, jest.config.js, .planning/codebase/TESTING.md, src/!tests/unit-tests/index.test.ts, src/!tests/unit-tests/schedule.test.ts, src/!tests/unit-tests/0 - utils/request.test.ts, src/!tests/unit-tests/jobs/prepareReplaysList/startFetchingReplays.test.ts, src/!tests/unit-tests/1 - replays, 2 - parseReplayInfo/workers/parseReplayWorker.test.ts</read_first>
  <files>package.json, vitest.config.ts, src/!tests/**/*.test.ts, src/!tests/**/*.snap, src/!tests/utils/**/*.ts</files>
  <behavior>
    - Test runs under Vitest with node environment, coverage via V8, and the existing `src/!tests` layout.
    - Jest-specific APIs are replaced with Vitest equivalents for mocks, spies, fake timers, module resets, and `importActual`.
    - Snapshot and orchestration tests remain deterministic and preserve behavioral intent.
  </behavior>
  <action>Remove Jest-only dependencies and commands from `package.json`, add `vitest` and `@vitest/coverage-v8`, and create `vitest.config.ts` with `test.environment = 'node'`, `globals = true`, `include = ['src/!tests/**/*.test.ts']`, and coverage thresholds matching the existing 100% global thresholds where still practical. Convert tests from `jest` globals/import assumptions to Vitest APIs: replace `jest.mock`/`jest.spyOn`/`jest.useFakeTimers`/`jest.setSystemTime`/`jest.resetModules`/`jest.requireActual` with `vi.mock`/`vi.spyOn`/`vi.useFakeTimers`/`vi.setSystemTime`/`vi.resetModules`/`vi.importActual`. Fix hoisting and import-order issues explicitly in the worker and prepare-replays suites. Keep the existing deterministic helper patterns under `src/!tests/utils` rather than rewriting the suite structure.</action>
  <verify>
    <automated>pnpm test && pnpm exec vitest run --coverage</automated>
  </verify>
  <acceptance_criteria>`vitest.config.ts` exists and contains `environment: 'node'`, `include: ['src/!tests/**/*.test.ts']`, and `provider: 'v8'`; `package.json` contains `"test": "vitest run --silent"` or equivalent Vitest command and contains no `jest` script; `rg -n "jest\\." src/!tests` returns no matches; `pnpm exec vitest run --coverage` exits 0.</acceptance_criteria>
  <done>Phase 1 ends in a Vitest-only state per D-11/D-12, with deterministic behavior-focused tests still covering the current parser flow.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CI, deploy, PM2, and README command surfaces to the final pnpm-first operator workflow</name>
  <read_first>README.md, .github/workflows/ci.yml, deploy/remote-deploy.sh, ecosystem.config.cjs, package.json, docs/architecture.md</read_first>
  <files>README.md, .github/workflows/ci.yml, deploy/remote-deploy.sh, ecosystem.config.cjs, package.json</files>
  <action>Update every operator-facing automation and document to the final Phase 1 command surface from D-03, D-04, D-05, and D-08. In `.github/workflows/ci.yml`, replace `npm ci`, `npm run lint`, `npm run tsc`, and `npm run test` with the pnpm equivalents, including Corepack or pnpm setup as needed, and add `pnpm build` to the modernized verification chain. In `deploy/remote-deploy.sh`, replace `npm ci` and `npm run build-dist` with `pnpm install --frozen-lockfile` and `pnpm build`. In `README.md`, replace every npm command with the final pnpm commands, document source-mode developer entrypoints and production build/deploy commands, and remove obsolete deprecation-era wording only if it conflicts with the new operator instructions. Keep PM2 pointed at the built scheduler entry and document that runtime explicitly. Do not leave any required operational step depending on npm semantics.</action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm build && ! rg -n "npm (run|ci)|\\bnpm\\b" README.md .github/workflows/ci.yml deploy/remote-deploy.sh ecosystem.config.cjs</automated>
  </verify>
  <acceptance_criteria>`README.md`, `.github/workflows/ci.yml`, and `deploy/remote-deploy.sh` contain `pnpm` commands and no required `npm` commands; `.github/workflows/ci.yml` contains a `pnpm build` step; `deploy/remote-deploy.sh` contains `pnpm install --frozen-lockfile` and `pnpm build`; `ecosystem.config.cjs` still contains `dist/schedule.js` or the documented replacement path.</acceptance_criteria>
  <done>Operators and maintainers can use the documented pnpm-first command surface end to end, and the final CI/deploy/runtime automation no longer depends on npm-specific behavior.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm typecheck && pnpm test && pnpm exec vitest run --coverage && pnpm build`. Then grep the README, CI workflow, deploy script, PM2 config, and package manifest to confirm the operational surface no longer depends on `npm`.
</verification>

<success_criteria>
The repository finishes Phase 1 with Vitest as the only supported test runner, CI/deploy/docs fully migrated to pnpm, and the operator command surface documented consistently across README, CI, deploy, and PM2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-modernization/01-toolchain-modernization-03-SUMMARY.md`
</output>
